Standard

```js padded
import MenuIcon from '@material-ui/icons/Menu'
;<ButtonCircle onClick={() => {}} title="Button Lable">
  <MenuIcon />
</ButtonCircle>
```

Primary

```js padded
import ScannerIcon from '@material-ui/icons/CropFree'
;<ButtonCircle onClick={() => {}} variant="primary" title="Button Lable">
  <ScannerIcon />
</ButtonCircle>
```

Small

```js padded
import AddShopIcon from '@material-ui/icons/PostAdd'
;<ButtonCircle onClick={() => {}} variant="small" title="Button Lable">
  <AddShopIcon />
</ButtonCircle>
```
